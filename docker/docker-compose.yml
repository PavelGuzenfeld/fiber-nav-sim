services:
  # Full simulation with Gazebo Harmonic GUI
  simulation:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: fiber-nav-sim
    privileged: true
    network_mode: host
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - QT_X11_NO_MITSHM=1
      - LIBGL_ALWAYS_SOFTWARE=0
      # WSLg support
      - WAYLAND_DISPLAY=${WAYLAND_DISPLAY:-wayland-0}
      - XDG_RUNTIME_DIR=/mnt/wslg/runtime-dir
      - PULSE_SERVER=/mnt/wslg/PulseServer
      # Gazebo Harmonic environment
      - GZ_SIM_RESOURCE_PATH=/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/models:/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/worlds
      # PX4 environment (optional)
      - PX4_SIM_MODEL=gz_x500
      - PX4_GZ_MODEL_POSE=0,0,50,0,0,0
    volumes:
      # X11 for GUI
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ${HOME}/.Xauthority:/root/.Xauthority:rw
      # WSLg mounts for Windows 11
      - /mnt/wslg:/mnt/wslg:rw
      # Source code (for live editing)
      - ../:/root/ws/src/fiber-nav-sim:rw
      # Data directory for recordings
      - ../data:/root/data:rw
    devices:
      - /dev/dri:/dev/dri
    stdin_open: true
    tty: true
    command: >
      bash -c "
        ros2 launch fiber_nav_bringup simulation.launch.py
      "

  # Standalone mode (no PX4, uses mock attitude, auto-fly enabled)
  # Headless rendering via EGL (no X11/GPU required)
  standalone:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: fiber-nav-standalone
    privileged: true
    network_mode: host
    environment:
      - GZ_SIM_RESOURCE_PATH=/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/models:/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/worlds
      - DISPLAY=
      - LIBGL_ALWAYS_SOFTWARE=1
      - MESA_GL_VERSION_OVERRIDE=3.3
    volumes:
      - ../:/root/ws/src/fiber-nav-sim:rw
      - ../data:/root/data:rw
    stdin_open: true
    tty: true
    command: >
      bash -c "
        ros2 launch fiber_nav_bringup simulation.launch.py use_px4:=false auto_fly:=true headless:=true
      "

  # PX4 SITL mode - launches Gazebo + sensors headless with quadtailsitter
  # PX4 + DDS + custom mode must be started manually via docker exec (needs real TTY)
  # See CLAUDE.md "Integration Testing (Manual)" for step-by-step instructions
  px4-sitl:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: fiber-nav-px4-sitl
    privileged: true
    network_mode: host
    environment:
      - GZ_SIM_RESOURCE_PATH=/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/models:/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/worlds
      - DISPLAY=
      - LIBGL_ALWAYS_SOFTWARE=1
      - MESA_GL_VERSION_OVERRIDE=3.3
      - PX4_SIM_MODEL=quadtailsitter
      - PX4_GZ_MODEL_NAME=quadtailsitter
    volumes:
      - ../:/root/ws/src/fiber-nav-sim:rw
      - ../data:/root/data:rw
    stdin_open: true
    tty: true
    command: >
      bash -c "
        ros2 launch fiber_nav_bringup simulation.launch.py use_px4:=true headless:=true
      "

  # Foxglove visualization - web-based ROS 2 visualization
  # Access at http://localhost:8765 with Foxglove Studio
  foxglove:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: fiber-nav-foxglove
    network_mode: host
    environment:
      - GZ_SIM_RESOURCE_PATH=/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/models:/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/worlds
    volumes:
      - ../:/root/ws/src/fiber-nav-sim:rw
    command: >
      bash -c "
        apt-get update && apt-get install -y ros-jazzy-foxglove-bridge &&
        source /opt/ros/jazzy/setup.bash &&
        source /root/ws/install/setup.bash &&
        ros2 launch foxglove_bridge foxglove_bridge_launch.xml port:=8765
      "

  # Analysis/Jupyter notebook service
  analysis:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: fiber-nav-analysis
    network_mode: host
    ports:
      - "8888:8888"
    volumes:
      - ../:/root/ws/src/fiber-nav-sim:rw
      - ../data:/root/data:rw
    command: >
      bash -c "
        pip install jupyter &&
        cd /root/data &&
        jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser --allow-root
      "

  # Headless simulation for CI/testing
  ci:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: fiber-nav-ci
    network_mode: host
    environment:
      - DISPLAY=:99
      - LIBGL_ALWAYS_SOFTWARE=1
      - MESA_GL_VERSION_OVERRIDE=3.3
      - GZ_SIM_RESOURCE_PATH=/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/models:/root/ws/src/fiber-nav-sim/src/fiber_nav_gazebo/worlds
    command: >
      bash -c "
        Xvfb :99 -screen 0 1024x768x24 &
        sleep 2 &&
        cd /root/ws &&
        source install/setup.bash &&
        colcon build --symlink-install --packages-skip px4_msgs px4_ros2_cpp px4_ros2_py --packages-skip-regex 'example_.*' &&
        colcon test --packages-skip px4_msgs px4_ros2_cpp px4_ros2_py --packages-skip-regex 'example_.*' &&
        colcon test-result --verbose
      "

  # Build and test only (no simulation)
  test:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: fiber-nav-test
    volumes:
      - ../:/root/ws/src/fiber-nav-sim:rw
    command: >
      bash -c "
        cd /root/ws &&
        source install/setup.bash &&
        colcon build --symlink-install --packages-skip px4_msgs px4_ros2_cpp px4_ros2_py --packages-skip-regex 'example_.*' &&
        colcon test --packages-skip px4_msgs px4_ros2_cpp px4_ros2_py --packages-skip-regex 'example_.*' &&
        colcon test-result --verbose
      "
